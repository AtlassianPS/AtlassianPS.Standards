paths:
  .github/workflows/module_release.yml:
    ignore:
      # actionlint 1.7.12 does not yet model GitHub's reusable-workflow identity fields.
      - 'property "workflow_(repository|sha)" is not defined in object type'
